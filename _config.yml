# Jupyter Book config
# Minimal defaults; tweak later (logo, theme, etc.)
title: AP Labs UFR Handbook
author: AP Labs UFR
only_build_toc_files: true
execute:
  execute_notebooks: "off"   # change to "auto" later if you want CI to run notebooks
repository:
  url: https://github.com/ap-labs-ufr/handbook
  branch: main
html:
  use_edit_page_button: true
  use_repository_button: true
  use_issues_button: true
